--[[ Provides eye-candy - used by nvim-notify and indirectly noice.nvim ]]

return {
   [1] = 'nvim-tree/nvim-web-devicons',
   opts = {
      color_icons = true,
      default = true,
      strict = true,
   },
}
